#include "main.h"
/**
 * positive_or_negative - show whether a number is positive or negative
 * @i: global variable
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negative", i);
	else
		printf("%d is positive", i);


}
